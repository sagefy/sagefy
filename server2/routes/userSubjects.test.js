describe('User-Subjects Route', () => {
  describe('GET /x/user/:userId/subjects', () => {
    test('list user subjects', () => {
      expect(true).toBe(false)
    })
  })

  describe('POST /x/user/:userId/subjects/:subjectId', () => {
    test('add a subject to user', () => {
      expect(true).toBe(false)
    })
  })

  describe('DELETE /x/user/:userId/subjects/:subjectId', () => {
    test('remove a subject from user', () => {
      expect(true).toBe(false)
    })
  })
})
