formFieldSelectTmpl =
    require('../../../app/views/components/form_field_select.tmpl.coffee')
expect = require('chai').expect

describe('form field select tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
