breadcrumbTmpl = require('../../../app/views/components/breadcrumb.tmpl.coffee')
expect = require('chai').expect

describe('breadcrumb tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
