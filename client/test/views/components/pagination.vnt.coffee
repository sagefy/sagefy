paginationVnt = require('../../../app/views/components/pagination.vnt.coffee')
expect = require('chai').expect

describe('pagination vnt', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
