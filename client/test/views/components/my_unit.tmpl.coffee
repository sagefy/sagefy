myUnitTmpl = require('../../../app/views/components/my_unit.tmpl.coffee')
expect = require('chai').expect

describe('my unit tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
