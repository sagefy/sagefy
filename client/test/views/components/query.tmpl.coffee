queryTmpl = require('../../../app/views/components/query.tmpl.coffee')
expect = require('chai').expect

describe('query tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
