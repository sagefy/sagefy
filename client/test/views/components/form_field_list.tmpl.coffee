formFieldListTmpl = require('../../../app/views/components/form_field_list.tmpl.coffee')
expect = require('chai').expect

describe('form field list tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
