entityVersionsTmpl =
    require('../../../app/views/components/entity_versions.tmpl.coffee')
expect = require('chai').expect

describe('entity versions tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
