breadcrumbVnt = require('../../../app/views/components/breadcrumb.vnt.coffee')
expect = require('chai').expect

describe('breadcrumb vnt', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
