formFieldInputTmpl =
    require('../../../app/views/components/form_field_input.tmpl.coffee')
expect = require('chai').expect

describe('form field input tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
