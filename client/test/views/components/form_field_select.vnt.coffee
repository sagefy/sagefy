formFieldSelectVnt =
    require('../../../app/views/components/form_field_select.vnt.coffee')
expect = require('chai').expect

describe('form field select vnt', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
