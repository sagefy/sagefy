formFieldSelectOptionTmpl = require('../../../app/views/components/' +
    'form_field_select_option.tmpl.coffee')
expect = require('chai').expect

describe('form field select option tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
