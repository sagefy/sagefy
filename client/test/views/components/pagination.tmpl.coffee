paginationTmpl = require('../../../app/views/components/pagination.tmpl.coffee')
expect = require('chai').expect

describe('pagination tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
