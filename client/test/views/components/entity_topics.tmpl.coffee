entityTopicsTmpl =
    require('../../../app/views/components/entity_topics.tmpl.coffee')
expect = require('chai').expect

describe('entity topics tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
