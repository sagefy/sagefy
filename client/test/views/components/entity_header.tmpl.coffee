entityHeaderTmpl =
    require('../../../app/views/components/entity_header.tmpl.coffee')
expect = require('chai').expect

describe('entity header tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
