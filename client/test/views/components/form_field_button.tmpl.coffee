formFieldButtonTmpl =
    require('../../../app/views/components/form_field_button.tmpl.coffee')
expect = require('chai').expect

describe('form field button tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
