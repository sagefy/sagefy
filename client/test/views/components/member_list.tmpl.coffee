memberListTmpl =
    require('../../../app/views/components/member_list.tmpl.coffee')
expect = require('chai').expect

describe('member list tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
