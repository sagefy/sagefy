formFieldLabelTmpl =
    require('../../../app/views/components/form_field_label.tmpl.coffee')
expect = require('chai').expect

describe('form field label tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
