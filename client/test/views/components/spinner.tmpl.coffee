spinnerTmpl = require('../../../app/views/components/spinner.tmpl.coffee')
expect = require('chai').expect

describe('spinner tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
