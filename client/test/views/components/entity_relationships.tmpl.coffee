entityRelationshipsTmpl =
    require('../../../app/views/components/entity_relationships.tmpl.coffee')
expect = require('chai').expect

describe('entity relationships tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
