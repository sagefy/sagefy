myUnitVnt = require('../../../app/views/components/my_unit.vnt.coffee')
expect = require('chai').expect

describe('my unit vnt', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
