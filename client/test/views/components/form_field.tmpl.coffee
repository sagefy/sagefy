formFieldTmpl = require('../../../app/views/components/form_field.tmpl.coffee')
expect = require('chai').expect

describe('form field tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
