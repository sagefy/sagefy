formFieldTextareaTmpl =
    require('../../../app/views/components/form_field_textarea.tmpl.coffee')
expect = require('chai').expect

describe('form field textarea tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
