followButtonVnt =
    require('../../../app/views/components/follow_button.vnt.coffee')
expect = require('chai').expect

describe('follow button vnt', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
