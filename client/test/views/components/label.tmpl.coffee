labelTmpl = require('../../../app/views/components/label.tmpl.coffee')
expect = require('chai').expect

describe('label tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
