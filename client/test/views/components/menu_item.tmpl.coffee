menuItemTmpl = require('../../../app/views/components/menu_item.tmpl.coffee')
expect = require('chai').expect

describe('menu item tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
