timeagoTmpl = require('../../../app/views/components/timeago.tmpl.coffee')
expect = require('chai').expect

describe('timeago tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
