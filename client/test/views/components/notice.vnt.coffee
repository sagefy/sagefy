noticeVnt = require('../../../app/views/components/notice.vnt.coffee')
expect = require('chai').expect

describe('notice vnt', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
