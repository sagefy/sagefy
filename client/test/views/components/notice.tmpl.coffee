noticeTmpl = require('../../../app/views/components/notice.tmpl.coffee')
expect = require('chai').expect

describe('notice tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
