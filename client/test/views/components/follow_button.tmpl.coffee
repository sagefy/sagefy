followButtonTmpl =
    require('../../../app/views/components/follow_button.tmpl.coffee')
expect = require('chai').expect

describe('follow button tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
