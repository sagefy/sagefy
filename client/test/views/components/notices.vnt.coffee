noticesVnt = require('../../../app/views/components/notices.vnt.coffee')
expect = require('chai').expect

describe('notices vnt', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
