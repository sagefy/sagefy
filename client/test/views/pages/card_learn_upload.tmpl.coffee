cardLearnUploadTmpl =
    require('../../../app/views/pages/card_learn_upload.tmpl.coffee')
expect = require('chai').expect

describe('card learn upload tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
