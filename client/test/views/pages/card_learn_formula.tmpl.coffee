cardLearnFormulaTmpl =
    require('../../../app/views/pages/card_learn_formula.tmpl.coffee')
expect = require('chai').expect

describe('card learn formula tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
