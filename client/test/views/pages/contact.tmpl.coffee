contactTmpl = require('../../../app/views/pages/contact.tmpl.coffee')
expect = require('chai').expect

describe('contact tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
