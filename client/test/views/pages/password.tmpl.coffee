passwordTmpl = require('../../../app/views/pages/password.tmpl.coffee')
expect = require('chai').expect

describe('password tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
