signUpTmpl = require('../../../app/views/pages/sign_up.tmpl.coffee')
expect = require('chai').expect

describe('sign up tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
