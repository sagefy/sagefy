cardLearnVideoTmpl =
    require('../../../app/views/pages/card_learn_video.tmpl.coffee')
expect = require('chai').expect

describe('card learn video tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
