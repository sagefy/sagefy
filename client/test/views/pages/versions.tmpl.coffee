versionsTmpl = require('../../../app/views/pages/versions.tmpl.coffee')
expect = require('chai').expect

describe('versions tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
