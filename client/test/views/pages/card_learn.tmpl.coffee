cardLearnTmpl = require('../../../app/views/pages/card_learn.tmpl.coffee')
expect = require('chai').expect

describe('card learn tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
