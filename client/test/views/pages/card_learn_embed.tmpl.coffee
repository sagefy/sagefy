cardLearnEmbedTmpl =
    require('../../../app/views/pages/card_learn_embed.tmpl.coffee')
expect = require('chai').expect

describe('card learn embed tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
