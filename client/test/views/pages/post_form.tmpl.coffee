postFormTmpl = require('../../../app/views/pages/post_form.tmpl.coffee')
expect = require('chai').expect

describe('post form tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
