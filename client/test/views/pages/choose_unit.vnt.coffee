chooseUnitVnt = require('../../../app/views/pages/choose_unit.vnt.coffee')
expect = require('chai').expect

describe('choose unit vnt', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
