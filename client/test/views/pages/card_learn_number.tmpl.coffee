cardLearnNumberTmpl =
    require('../../../app/views/pages/card_learn_number.tmpl.coffee')
expect = require('chai').expect

describe('card learn number tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
