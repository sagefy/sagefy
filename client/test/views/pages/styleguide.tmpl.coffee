styleguideTmpl = require('../../../app/views/pages/styleguide.tmpl.coffee')
expect = require('chai').expect

describe('styleguide tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
