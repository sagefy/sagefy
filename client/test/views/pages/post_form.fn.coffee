postFormFn = require('../../../app/views/pages/post_form.fn.coffee')
expect = require('chai').expect

describe('post form fn', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
