cardLearnAudioTmpl =
    require('../../../app/views/pages/card_learn_audio.tmpl.coffee')
expect = require('chai').expect

describe('card learn audio tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
