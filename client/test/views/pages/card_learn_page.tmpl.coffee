cardLearnPageTmpl =
    require('../../../app/views/pages/card_learn_page.tmpl.coffee')
expect = require('chai').expect

describe('card learn page tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
