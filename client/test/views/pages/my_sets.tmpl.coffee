mySetsTmpl = require('../../../app/views/pages/my_sets.tmpl.coffee')
expect = require('chai').expect

describe('my sets tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
