cardLearnSlideshowTmpl =
    require('../../../app/views/pages/card_learn_slideshow.tmpl.coffee')
expect = require('chai').expect

describe('card learn slideshow tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
