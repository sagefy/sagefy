topicFormTmpl = require('../../../app/views/pages/topic_form.tmpl.coffee')
expect = require('chai').expect

describe('topic form tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
