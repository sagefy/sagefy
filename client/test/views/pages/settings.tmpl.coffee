settingsTmpl = require('../../../app/views/pages/settings.tmpl.coffee')
expect = require('chai').expect

describe('settings tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
