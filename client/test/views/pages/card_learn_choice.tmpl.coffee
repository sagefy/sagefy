cardLearnChoiceTmpl =
    require('../../../app/views/pages/card_learn_choice.tmpl.coffee')
expect = require('chai').expect

describe('card learn choice tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
