passwordVnt = require('../../../app/views/pages/password.vnt.coffee')
expect = require('chai').expect

describe('password vnt', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
