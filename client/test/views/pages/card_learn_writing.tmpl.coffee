cardLearnWritingTmpl =
    require('../../../app/views/pages/card_learn_writing.tmpl.coffee')
expect = require('chai').expect

describe('card learn writing tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
