settingsVnt = require('../../../app/views/pages/settings.vnt.coffee')
expect = require('chai').expect

describe('settings vnt', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
