postFormVnt = require('../../../app/views/pages/post_form.vnt.coffee')
expect = require('chai').expect

describe('post form vnt', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
