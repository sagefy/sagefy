topicFormVnt = require('../../../app/views/pages/topic_form.vnt.coffee')
expect = require('chai').expect

describe('topic form vnt', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
