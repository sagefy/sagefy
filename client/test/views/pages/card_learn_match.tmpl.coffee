cardLearnMatchTmpl =
    require('../../../app/views/pages/card_learn_match.tmpl.coffee')
expect = require('chai').expect

describe('card learn match tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
