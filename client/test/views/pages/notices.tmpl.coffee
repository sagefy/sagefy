noticesTmpl = require('../../../app/views/pages/notices.tmpl.coffee')
expect = require('chai').expect

describe('notices tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
