cardLearnVnt = require('../../../app/views/pages/card_learn.vnt.coffee')
expect = require('chai').expect

describe('card learn vnt', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
