chooseUnitTmpl = require('../../../app/views/pages/choose_unit.tmpl.coffee')
expect = require('chai').expect

describe('choose unit tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
