profileTmpl = require('../../../app/views/pages/profile.tmpl.coffee')
expect = require('chai').expect

describe('profile tmpl', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
