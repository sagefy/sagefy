choiceCard = require('../../../app/schemas/cards/choice_card.coffee')
expect = require('chai').expect

describe('choice card', ->
    it.skip('needs tests', ->
        expect(false).to.be.true
    )
)
