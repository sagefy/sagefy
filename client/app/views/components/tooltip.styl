/*
---
title: Tooltip
description: Hover over an element with `data-title` to view the contents. No JavaScript required! TODO@ virtual hyper doesn't work well with data-*
example: |
    <a href="#" data-title="Auto tooltip">An example</a> of a tooltip
---
*/

[data-title]
    position relative
    cursor help

    a&
        cursor pointer

    &:after
        background $base-color(43)
        color $base-color(99)
        @extend .base-font
        position absolute
        opacity 0
        pointer-events none
        content attr(data-title)
        padding 0 ($unit / 2)
        white-space nowrap
        z-index 2
        top 100%
        min-width 100%
        left 0
        border-radius $border-radius
        transition opacity 0.2s

    &:hover:after
        opacity 0.8
