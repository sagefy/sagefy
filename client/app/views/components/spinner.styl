/*
---
title: Spinners
description: |
    A placeholder element to wait for things to load.
---
*/

@keyframes spinner
    0%
        transform scale(0)
        opacity 0
    50%
        transform scale(1)
        opacity 0.5
    100%
        transform scale(0)
        opacity 0

.spinner
    width $unit * 4
    height $unit * 4
    position relative
    margin 0 auto

    &:before, &:after
        content ' '
        display block
        border-radius 100%
        width 100%
        height 100%
        position absolute
        opacity 0

    &:before
        animation spinner 2.2s ease-in-out 0.3s infinite
        background $accent-color(50)

    &:after
        animation spinner 1.6s ease-in-out 0s infinite
        background $good-color(50)
