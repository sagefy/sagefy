language: python
python:
    - 2.7
install:
    - pip install -r setup/requirements.txt && npm install -g gulp && cd ui && npm install && cd ..
script:
    - py.test api/tests/ && cd ui && gulp test && cd ..
after_success:
    coveralls
