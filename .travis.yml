language: python
python:
    - "2.7"
before_install:
    - "cd api"
install: pip install -r requirements.txt --use-mirrors
script:

language: node_js
node_js:
    - "0.10"
before_install:
    - npm install -g bower grunt-cli
    - npm install
    - "cd ui"

notifications:
    email: false