require('../../app/adapters/menu')

describe('Menu Adapter', ->
    it.skip('should create the menu view', ->

    )

    it.skip('should rerender the view on state change', ->

    )

    it.skip('should remove the view', ->

    )
)
