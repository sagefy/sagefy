describe('post_form.vnt', ->
    it.skip('should have "create" in the title in create mode', ->

    )

    it.skip('should have "update" in the title in update mode', ->

    )

    it.skip('should link to the related entity', ->

    )

    describe('Kind', ->
        it.skip('should allow me to choose a post kind', ->

        )

        it.skip('should update fields depending on kind selected', ->

        )

        it.skip('should swap post models on changing kind selected', ->

        )
    )

    describe('Body', ->
        it.skip('should provide a post body field', ->

        )

        it.skip('should require a post body if post, proposal, or flag', ->

        )

        it.skip('should have post body optional if a vote', ->

        )

        it.skip('should show me a preview of my post body', ->

        )
    )

    describe('Proposal', ->
        it.skip('should ask for name', ->

        )

        it.skip('should ask for action kind', ->

        )

        it.skip('should ask for status', ->

        )

        it.skip('should show card fields', ->

        )

        it.skip('should show unit fields', ->

        )

        it.skip('should show set fields', ->

        )
    )

    describe('Vote', ->
        it.skip('should ask for a response (yes, no, discuss)', ->

        )
    )

    describe('Flag', ->
        it.skip('should ask for a reason', ->

        )
    )
)
