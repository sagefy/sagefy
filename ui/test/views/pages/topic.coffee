require('../../scripts/adapters/topic')

describe('Topic Adapter', ->
    it.skip('should show the kind and name of the entity', ->

    )

    it.skip('should show the name of the topic', ->

    )

    it.skip('should show a button to follow the entity', ->

    )

    it.skip('should follow an entity', ->

    )

    it.skip('should unfollow an entity', ->

    )

    it.skip('should show a new post button', ->

    )

    describe('Posts and Scroll', ->
        it.skip('should show a list of posts', ->

        )

        it.skip('should infinitely scroll the list of posts', ->

        )

        it.skip('should update the counter as I scroll through posts', ->

        )

        it.skip('should let me update the current post number', ->

        )
    )

    describe('A post', ->
        it.skip('should show a user avatar for the post', ->

        )

        it.skip('should show the user name', ->

        )

        it.skip('should show time ago since post', ->

        )

        it.skip('should link to post replied to', ->

        )

        it.skip('should show the post body', ->

        )

        it.skip('should show the body in HTML (mdified)', ->

        )

        it.skip('should have an edit link if own post', ->

        )

        it.skip('should not have edit link if not own post', ->

        )

        it.skip('should show reply link', ->

        )

        it.skip('should show vote link only if proposal', ->

        )

        it.skip('shuold show share link', ->

        )

        it.skip('should show flag link if not own post', ->

        )
    )

    describe('A proposal', ->
        it.skip('should show proposal name', ->

        )

        it.skip('should show proposal action', ->

        )

        it.skip('should show proposal status', ->

        )

        it.skip('should sum the proposal diff', ->

        )
    )

    describe('A vote', ->
        it.skip('should show the vote action', ->

        )
    )

    describe('A flag', ->
        it.skip('should show the flag reason', ->

        )

        it.skip('should show the flag status', ->

        )
    )
)
