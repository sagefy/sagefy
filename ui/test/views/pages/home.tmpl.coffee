require('../../../app/views/pages/home.tmpl')

# TODO outline tests

describe('Home Template', ->
    it.skip('should redirect to my sets if logged in', ->

    )

    it.skip('should create and render the index view', ->

    )

    it.skip('should remove the view', ->

    )
)
