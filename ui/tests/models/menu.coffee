MenuModel = require('../../scripts/models/menu')

describe('Menu Model', ->
    it.skip('should generate a list of menu items', ->
    )

    it.skip('should have the right menu items on logged out', ->
    )

    it.skip('should have the right menu items when logged in', ->
    )

    it.skip('should update the menu items when the state changes', ->
    )

    it.skip('should allow setting a custom icon', ->
    )

    it.skip('should capitalize the page title', ->
    )

    it.skip('should use underscores for URLs', ->
    )
)
