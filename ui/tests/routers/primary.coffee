describe.skip('PrimaryRouter', ->
    it('should add the menu view', ->)
    it('should navigate internal links using pushState', ->)
    it('should create the page element', ->)
    it('should 404 if the URL doesn\'t match', ->)
)
