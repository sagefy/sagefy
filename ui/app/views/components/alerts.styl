/*
---
title: Alerts & Errors
description: Systemic-alerts are often used when an error has occured. Alerts are fixed to the page until a user chooses to close them. Very serious alerts can force the user to reload the page. This situation calls for a modal.
elements:
    - div (root)
    - a.alert__close
modifiers:
    - (default)
    - good
    - bad
    - accent
example: |
    <div class="alert">
        We are sorry, but an error has occurred. Please reload the page.
        <a href="#" class="alert__close">Close</a>
    </div>

    <div class="alert--good">
        We are sorry, but an error has occurred. Please reload the page.
        <a href="#" class="alert__close">Close</a>
    </div>

    <div class="alert--bad">
        We are sorry, but an error has occurred. Please reload the page.
        <a href="#" class="alert__close">Close</a>
    </div>

    <div class="alert--accent">
        We are sorry, but an error has occurred. Please reload the page.
        <a href="#" class="alert__close">Close</a>
    </div>
---
*/


.alert
    border-radius $border-radius
    display block
    padding 0 ($unit / 2)
    margin $unit 0
    background $base-color(90)
    color $base-color(43)

    .alert__close
        float right
        color $base-color(43)
        border-color $base-color(43)

.alert--bad
    @extend .alert
    background $bad-color(90)
    color $bad-color(43)

    .alert__close
        color $bad-color(43)
        border-color $bad-color(43)

.alert--good
    @extend .alert
    background $good-color(90)
    color $good-color(43)

    .alert__close
        color $good-color(43)
        border-color $good-color(43)

.alert--accent
    @extend .alert
    background $accent-color(90)
    color $accent-color(43)

    .alert__close
        color $accent-color(43)
        border-color $accent-color(43)
