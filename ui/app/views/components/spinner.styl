/*
---
title: Spinners
description: |
    A placeholder element to wait for things to load.
example: |
    <div class="spinner"></div>
---
*/

@keyframes spinner
    0%
        transform scale(0)
        opacity 0
    80%
        transform scale(1)
        opacity 0.5
    100%
        opacity 0

.spinner
    width $unit * 4
    height $unit * 4
    position relative

    &:before, &:after
        content ' '
        display block
        border-radius 100%
        width 100%
        height 100%
        position absolute
        background $accent-color(50)
        opacity 0

    &:before
        animation spinner 2.2s ease-in-out 0.3s infinite

    &:after
        animation spinner 1.6s ease-in-out 0s infinite
