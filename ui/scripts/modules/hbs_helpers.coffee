
# Strings >> lowercase
# Strings >> uppercase
# Strings >> ucfirst
# Strings >> titlecase
# Strings >> sentencecase
# Strings >> dashify
# Strings >> truncate

# Arrays >> first
# Arrays >> last
# Arrays >> after
# Arrays >> before
# Arrays >> eachIndex
# Arrays >> eachProperty
# Arrays >> join
# Arrays >> joinSentence
# Arrays >> contains
# Arrays >> length

# Comparisons >> &&
# Comparisons >> ||
# Comparisons >> in
# Comparisons >> >
# Comparisons >> >=
# Comparisons >> <
# Comparisons >> <=
# Comparisons >> ==
# Comparisons >> !=

# formatDate (moment)
# markdown
# formatNumber
# default
# translate
